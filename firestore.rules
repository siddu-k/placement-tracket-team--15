rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // ALLOW ALL ACCESS (Development Only - NOT for Production!)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
